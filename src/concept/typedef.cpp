#include <bits/stdc++.h>
using namespace std;
/**
 * 타입의 이름을 새로운 별칭으로 정의할 수 있다.
 * 이를 톹해 C++ 에서 이미 정의된 타입 또는 사용자가 정의한 타입보다
 * 더 짧거나 의미 있는 이름을 지을 수 있다.
 */
typedef int i;
int main()
{
    i a = 1;
    cout << a << '\n';
    return 0;
}