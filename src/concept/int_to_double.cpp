/* C++의 모든 표준 라이브러리가 포함된 헤더파일, include 해준다. */
#include <bits/stdc++.h>

using namespace std;
int a, b;
double c;
int main()
{
    scanf("%d.%d", &a, &b);
    printf("\n%d %d\n", a, b);

    scanf("%lf", &c);
    printf("%lf\n", c);
}